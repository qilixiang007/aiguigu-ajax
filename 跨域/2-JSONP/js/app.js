const data={
    name:'尚硅谷'
};

//console.log(data);

// 处理数据  （把name属性里的值放在div里）
// function handle(data){
//     // 获取result元素
//     const result=document.getElementById('result');
//     result.innerHTML=data.name;
// }
handle(data);
